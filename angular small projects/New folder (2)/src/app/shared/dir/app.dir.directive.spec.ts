import { App.DirDirective } from './app.dir.directive';

describe('App.DirDirective', () => {
  it('should create an instance', () => {
    const directive = new App.DirDirective();
    expect(directive).toBeTruthy();
  });
});
